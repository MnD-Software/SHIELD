const menu=document.querySelector('.menu-button'),nav=document.querySelector('.navlinks');if(menu){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)})}document.querySelectorAll('.toast').forEach((toast,i)=>setTimeout(()=>toast.classList.add('hide'),3500+i*400));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.1});document.querySelectorAll('.reveal,.product-card,.category-card').forEach(el=>observer.observe(el));
const drawer=document.querySelector('.filter-drawer'),overlay=document.querySelector('.filter-overlay'),trigger=document.querySelector('.filter-trigger'),closeFilter=()=>{if(!drawer)return;drawer.classList.remove('open');drawer.setAttribute('aria-hidden','true');overlay.hidden=true;trigger?.setAttribute('aria-expanded','false');document.body.classList.remove('no-scroll')};if(trigger){trigger.addEventListener('click',()=>{drawer.classList.add('open');drawer.setAttribute('aria-hidden','false');overlay.hidden=false;trigger.setAttribute('aria-expanded','true');document.body.classList.add('no-scroll')});document.querySelector('.filter-close').addEventListener('click',closeFilter);overlay.addEventListener('click',closeFilter);document.addEventListener('keydown',e=>{if(e.key==='Escape')closeFilter()})}
const steps=[...document.querySelectorAll('.checkout-step')],stepNav=[...document.querySelectorAll('[data-step-nav]')];let currentStep=1;const showStep=n=>{currentStep=n;steps.forEach(s=>s.classList.toggle('active',Number(s.dataset.step)===n));stepNav.forEach(b=>b.classList.toggle('active',Number(b.dataset.stepNav)<=n));window.scrollTo({top:0,behavior:'smooth'});if(n===3){const form=document.querySelector('.progressive-form'),value=name=>form.elements[name]?.value||'';document.querySelector('[data-review="address"]').textContent=[value('address'),value('town'),value('county')].filter(Boolean).join(', ');document.querySelector('[data-review="contact"]').textContent=value('phone');document.querySelector('[data-review="payment"]').textContent=value('payment_method')==='cash'?'Cash on delivery':'M-Pesa'}};document.querySelectorAll('.next-step').forEach(button=>button.addEventListener('click',()=>{const active=document.querySelector(`.checkout-step[data-step="${currentStep}"]`),required=[...active.querySelectorAll('[required]')];if(required.some(input=>!input.reportValidity()))return;showStep(Math.min(3,currentStep+1))}));document.querySelectorAll('.step-back').forEach(button=>button.addEventListener('click',()=>showStep(Math.max(1,currentStep-1))));stepNav.forEach(button=>button.addEventListener('click',()=>{const target=Number(button.dataset.stepNav);if(target<currentStep)showStep(target)}));
const adminModal=document.querySelector('.admin-modal'),adminForm=document.querySelector('.admin-product-form');const closeAdmin=()=>{if(adminModal)adminModal.hidden=true};document.querySelectorAll('[data-admin-close]').forEach(button=>button.addEventListener('click',closeAdmin));document.querySelectorAll('[data-admin-open]').forEach(button=>button.addEventListener('click',()=>{adminForm?.reset();if(adminForm)adminForm.elements.product_id.value='';document.querySelector('[data-form-title]').textContent='Add product';adminModal.hidden=false}));document.querySelectorAll('.admin-edit').forEach(button=>button.addEventListener('click',()=>{const product=JSON.parse(button.dataset.product);Object.entries(product).forEach(([key,value])=>{const field=adminForm?.elements[key];if(!field)return;if(field.type==='checkbox')field.checked=Boolean(value);else field.value=value??''});document.querySelector('[data-form-title]').textContent='Edit product';adminModal.hidden=false}));
const adminSidebar=document.querySelector('.admin-sidebar'),adminMenuToggle=document.querySelector('.admin-menu-toggle');adminMenuToggle?.addEventListener('click',()=>adminSidebar?.classList.toggle('open'));document.querySelectorAll('.admin-sidebar nav a').forEach(link=>link.addEventListener('click',()=>adminSidebar?.classList.remove('open')));
