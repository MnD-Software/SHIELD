# Shared static assets

Brand-approved shared assets belong here. Storefront-specific assets should remain in `frontend/public`, while protected user-generated files must use managed object storage in production rather than this repository.

