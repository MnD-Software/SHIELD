"use client";
import Link from "next/link";import type {Product} from "@/lib/types";import {ProductCard} from "./product-card";import {useCart} from "./cart-provider";
export function WishlistClient({products}:{products:Product[]}){const {wishlist}=useCart();const saved=products.filter(product=>wishlist.includes(product.id));return saved.length?<div className="grid grid-cols-2 gap-4 md:grid-cols-4 md:gap-6">{saved.map(product=><ProductCard key={product.id} product={product}/>)}</div>:<div className="rounded-3xl border border-dashed bg-white px-6 py-20 text-center"><h2 className="text-2xl font-semibold">Nothing saved yet.</h2><p className="mt-3 text-sm text-muted">Use the heart on any product to keep it here.</p><Link href="/shop" className="primary mt-7">Explore products</Link></div>}
