import type {Metadata} from "next";import Link from "next/link";import {getCategories} from "@/lib/api";
export const metadata:Metadata={title:"Health categories"};
export default async function Categories(){const categories=await getCategories();return <section className="py-16 md:py-24"><div className="container-page"><span className="eyebrow">Explore the pharmacy</span><h1 className="mt-4 text-display">Care, organized clearly.</h1><p className="mt-5 max-w-xl text-sm leading-7 text-muted">Browse by health need and see availability before adding anything to your bag.</p><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{categories.map((category,index)=><Link href={`/shop?category=${category.slug}`} key={category.id} className="group min-h-56 rounded-3xl border bg-white p-7 transition duration-360 ease-luxury hover:-translate-y-1 hover:shadow-lift"><span className="text-xs text-muted">0{index+1}</span><i className="mt-8 block text-3xl not-italic">{category.icon}</i><h2 className="mt-5 text-xl font-semibold">{category.name}</h2><p className="mt-2 text-xs leading-6 text-muted">{category.description}</p><b className="mt-6 block text-xs text-shield-600">Explore {category.product_count} products →</b></Link>)}</div></div></section>}
